<label class="btn-add" role="button" tabindex="0">
	Add Files
	<input type="file" accept=".smc,.sfc,.zip" multiple id="file-input" aria-label="Add ROM files" />
</label>

<style>
	.btn-add {
		display: inline-flex;
		align-items: center;
		padding: 8px 16px;
		margin: 1rem;
		color: #ffffff;
		background: transparent;
		border: none;
		font-size: 14px;
		font-weight: 500;
		text-transform: uppercase;
		cursor: pointer;
		border-radius: 2px;
		letter-spacing: 0.5px;
	}

	.btn-add:hover {
		background: rgba(255, 255, 255, 0.1);
	}

	.btn-add input[type="file"] {
		display: none;
	}

	@media (max-width: 480px) {
		.btn-add {
			padding: 8px 10px;
			margin: 0.5rem;
			font-size: 12px;
		}
	}
</style>
