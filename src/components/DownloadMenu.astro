<div class="download-wrapper">
	<button class="btn-icon" id="download-btn" disabled title="Download ROMs">
		<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="#ffffff" width="24" height="24">
			<path d="M19 9h-4V3H9v6H5l7 7 7-7zM5 18v2h14v-2H5z"/>
		</svg>
	</button>
	<div class="download-menu" id="download-menu">
		<button class="download-option" data-headers="false">no headers (.sfc)</button>
		<button class="download-option" data-headers="true">with headers (.smc)</button>
	</div>
</div>

<style>
	.download-wrapper {
		position: relative;
	}

	.btn-icon {
		background: transparent;
		border: none;
		cursor: pointer;
		padding: 12px;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		opacity: 1;
		transition: opacity 0.2s;
	}

	.btn-icon:disabled {
		opacity: 0.4;
		cursor: default;
	}

	.btn-icon:not(:disabled):hover {
		background: rgba(255, 255, 255, 0.1);
	}

	.download-menu {
		display: none;
		position: absolute;
		right: 0;
		top: 100%;
		background: #ffffff;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
		border-radius: 2px;
		min-width: 180px;
		z-index: 10;
	}

	.download-menu.open {
		display: block;
	}

	.download-option {
		display: block;
		width: 100%;
		padding: 12px 16px;
		border: none;
		background: transparent;
		text-align: left;
		font-size: 14px;
		font-family: 'Roboto', sans-serif;
		cursor: pointer;
		color: rgba(0, 0, 0, 0.87);
	}

	.download-option:hover {
		background: rgba(0, 0, 0, 0.08);
	}
</style>
